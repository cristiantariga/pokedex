import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  description: {
    padding: 10,
  },
  textDescription: {
    fontSize: 18,
    textAlign: 'center',
    fontWeight: '600',
  },
});
